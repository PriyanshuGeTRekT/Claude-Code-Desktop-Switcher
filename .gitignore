# Nothing this tool creates at runtime belongs in the repo. Profiles, settings and
# logs all live under %LOCALAPPDATA%\ClaudeProfiles, not here, so this is mostly
# a guard against stray local files.

*.log
*.lnk
settings.json

# Windows / editor noise
Thumbs.db
desktop.ini
.vs/
.vscode/
*.swp
*~
